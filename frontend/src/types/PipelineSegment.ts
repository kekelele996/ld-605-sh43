export interface PipelineSegment {
  id: number;
  segment_code: string;
  district: string;
  material: string;
  diameter: string;
  install_year: string;
  pressure_zone: string;
  risk_level: string;
}
