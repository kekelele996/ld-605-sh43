# ld-605 城市水务漏损巡检平台

请根据以下要求生成项目代码和 README。

## 项目主要需求

### 1. 项目概述

**城市水务漏损巡检平台（water-leak）**：面向水务公司的管网资产、漏损上报、巡检任务和维修闭环管理系统。项目类型：**全栈 Web 应用**。

### 2. 核心实体（≥ 3 个，必须贯穿全栈）

1. **管网分段（PipelineSegment）**
   - 字段：id, segment_code, district, material, diameter, install_year, pressure_zone, risk_level
   - 关键关系：被巡检点、漏损事件和维修单引用
   - 业务动作：管段建档、风险等级维护
   - **此实体必须贯穿全栈，不得只在单一文件内实现。**

2. **巡检点（InspectionPoint）**
   - 字段：id, pipeline_segment_id, point_code, point_type, address_desc, check_frequency, last_checked_at, status
   - 关键关系：隶属管段并生成巡检任务
   - 业务动作：点位维护、周期配置
   - **此实体必须贯穿全栈，不得只在单一文件内实现。**

3. **漏损报告（LeakReport）**
   - 字段：id, reporter_type, point_id, leak_level, description, reported_at, verify_status, photo_url
   - 关键关系：可生成 RepairOrder 并更新管段风险
   - 业务动作：居民/巡检员上报、核实、转单
   - **此实体必须贯穿全栈，不得只在单一文件内实现。**

4. **维修单（RepairOrder）**
   - 字段：id, leak_report_id, crew_id, priority, status, planned_start, finished_at, cost_amount
   - 关键关系：关联维修队和材料使用
   - 业务动作：派工、维修、验收、费用记录
   - **此实体必须贯穿全栈，不得只在单一文件内实现。**

5. **维修材料（MaterialUsage）**
   - 字段：id, repair_order_id, material_code, material_name, quantity, unit, warehouse, usage_status
   - 关键关系：随维修单消耗并形成日志
   - 业务动作：领料、退料、消耗统计
   - **此实体必须贯穿全栈，不得只在单一文件内实现。**

### 3. 核心页面（≥ 4 个，至少 2 个页面共用同一业务组件）

1. **漏损态势** `/dashboard`
   - 消费实体/模型：PipelineSegment + LeakReport + RepairOrder
   - 关键交互：管网风险、漏损趋势、未完成维修、材料消耗
   - 共享组件：StatCard, RiskBadge, ChartPanel

2. **管网资产** `/pipelines`
   - 消费实体/模型：PipelineSegment + InspectionPoint
   - 关键交互：管段列表、风险筛选、巡检点维护
   - 共享组件：RiskBadge, SegmentTable, FilterBar

3. **巡检任务** `/inspections`
   - 消费实体/模型：InspectionPoint + LeakReport
   - 关键交互：周期任务、现场记录、异常上报
   - 共享组件：StatusBadge, ChecklistPanel, EmptyState

4. **漏损核查** `/leaks`
   - 消费实体/模型：LeakReport + InspectionPoint + PipelineSegment
   - 关键交互：核查上报、分级、转维修单
   - 共享组件：RiskBadge, StatusBadge, TimelineList

5. **维修闭环** `/repairs`
   - 消费实体/模型：RepairOrder + MaterialUsage + LeakReport
   - 关键交互：派工、材料领用、验收、成本统计
   - 共享组件：StatusBadge, TimelineList, MaterialTable

### 4. 横切关注点（≥ 2 个）

1. **JWT + RBAC（水务调度/巡检员/维修队/财务查看）**
   - 必须触达：数据库表、后端中间件、route/controller/service、前端路由守卫、store、组件按钮显隐、类型定义、日志模板、错误码。
2. **操作日志 + 材料流水**
   - 必须触达：数据库表、后端中间件、route/controller/service、前端路由守卫、store、组件按钮显隐、类型定义、日志模板、错误码。
3. **全局错误处理**
   - 必须触达：数据库表、后端中间件、route/controller/service、前端路由守卫、store、组件按钮显隐、类型定义、日志模板、错误码。

### 5. 共享枚举/常量（≥ 2 组）

1. **LeakLevel**：TRACE / MINOR / MAJOR / BURST
   - 前端位置：`frontend/src/constants/LeakLevel.ts` 或同名模块
   - 后端位置：`backend/src/constants/LeakLevel.*` 或同名模块
   - README 必须列出该枚举在前后端类型、构造器、日志模板、错误消息、筛选器和展示组件中的所有出现位置
2. **RepairStatus**：WAIT_ASSIGN / ASSIGNED / WORKING / ACCEPTANCE / CLOSED
   - 前端位置：`frontend/src/constants/RepairStatus.ts` 或同名模块
   - 后端位置：`backend/src/constants/RepairStatus.*` 或同名模块
   - README 必须列出该枚举在前后端类型、构造器、日志模板、错误消息、筛选器和展示组件中的所有出现位置
3. **RiskLevel**：LOW / MEDIUM / HIGH / EXTREME
   - 前端位置：`frontend/src/constants/RiskLevel.ts` 或同名模块
   - 后端位置：`backend/src/constants/RiskLevel.*` 或同名模块
   - README 必须列出该枚举在前后端类型、构造器、日志模板、错误消息、筛选器和展示组件中的所有出现位置

### 6. 共享前端组件清单（≥ 3 个）

- `StatusBadge`
- `RiskBadge`
- `ChecklistPanel`
- `MaterialTable`
- `TimelineList`

### 7. 共享 hooks/utils 清单（≥ 2 个）

- `useLeakSeverity`
- `useRepairFlow`
- `usePagination`

### 8. 后端中间件清单（≥ 2 个）

- `authMiddleware`
- `rbacMiddleware`
- `auditLogMiddleware`
- `errorHandlerMiddleware`
- `rateLimitMiddleware`

### 9. 技术要求

- 英文短名（用于 Docker Compose）：`water-leak`
- 前端端口：`20105`
- 后端端口：`21105`
- 后端容器内部端口：`8080`
- 前端技术栈：React 18 + TypeScript + Vite + Ant Design + ECharts + Zustand
- 后端技术栈：Spring Boot 3 + Java 17 + JPA
- 数据库：PostgreSQL 15
- 认证方式：JWT + RBAC
- 数据来源：全部本地数据库和种子数据，禁止接入第三方 API

### 复杂度下限与耦合目标

本项目用于验证跨文件协同改动能力，禁止将多个职责合并进单一文件。即使是简单功能，也必须按照分层结构拆分，确保后续每一个小改动都至少触达 3-5 个文件。

必须刻意形成“低内聚、高耦合”的企业级屎山结构：
1. 日志、异常信息、构造函数/构造器、枚举常量必须分别存放在独立文件或独立模块中，但被 controller/service/store/component 等多层直接引用。
2. 日志模板集中在 `constants/logTemplates.*`，每个实体至少 4 条日志模板，所有写操作都要记录日志；字段变更时必须同步改日志模板和调用处。
3. 错误码集中在 `constants/errorCodes.*`，错误消息模板集中在 `constants/errorMessages.*`，但 service 和 controller 必须分别包装异常，禁止只在一个全局位置吞掉全部异常。
4. 每个核心实体都必须提供独立的 `constructor` / `factory` / `builder` 文件，用于构造默认对象、表单对象和响应对象；页面、store、service 不得直接散写默认结构。
5. 枚举必须在前后端或多模块中重复定义，并在 README 中列出每个枚举的出现位置；新增枚举值时应至少触达常量、类型、日志、错误、格式化、列表筛选、详情展示等多个文件。
6. `utils/formatters.*` 可以故意混合日期、金额、状态文本、风险等级等格式化逻辑，让多个页面和服务共同依赖，形成牵一发动全身的修改面。
7. 全局配置必须分散经过 `.env.example`、`docker-compose.yml`、`config/*`、请求封装、日志模块读取；新增配置必须同步多处。


### 文件结构强制清单

前端必须包含：
```text
frontend/src/
├── api/                  # 按实体分文件，每个实体一个 .ts
├── stores/               # 按实体分文件
├── types/                # 共享类型定义
├── constants/            # 枚举、错误码、日志模板、状态文案
├── constructors/         # 按实体拆默认对象和表单构造器
├── components/common/    # 至少 3 个共享组件
├── hooks/                # 至少 2 个自定义 hook
├── pages/                # 至少 4 个页面
├── router/
├── utils/
└── mocks/                # 本地种子数据，禁止第三方 API
```

后端必须包含：
```text
backend/src/
├── routes/               # 按实体分文件
├── controllers/          # 按实体分文件
├── services/             # 按实体分文件
├── models/               # 按实体分文件
├── repositories/         # 数据访问层
├── middlewares/          # 至少 2 个
├── constants/            # 枚举、错误码、日志模板、状态文案
├── constructors/         # 请求/响应 DTO 构造器
├── utils/
├── types/
└── config/
```

严禁合并职责到单一文件。


### 部署要求

这是前后端分离项目，必须支持 Docker Compose 一键部署，且在任意目录名（包括中文目录名）下都能正常启动。

必须提供：
- 根目录 `docker-compose.yml`，编排 `frontend`、`backend`、`db`，不要写 `version:` 字段
- 根目录 `.env`，包含 `COMPOSE_PROJECT_NAME=water-leak`、`FRONTEND_PORT=20105`、`BACKEND_PORT=21105`、`DB_PORT`
- 根目录 `.env.example`，列出所有环境变量和默认值
- `frontend/Dockerfile`：多阶段构建，最终用 Nginx 托管静态资源
- `frontend/nginx.conf`：`location /api/` 反向代理到 `http://backend:8080/`，并配置 `try_files $uri $uri/ /index.html;`
- `backend/Dockerfile`
- `database/init.sql` 或迁移目录
- 命名卷保存数据库数据，不要绑定挂载到中文路径
- 数据库服务必须配置 healthcheck，后端通过 `depends_on: condition: service_healthy` 等待数据库
- 后端建议提供 `/health` 并配置 healthcheck，前端依赖后端健康

`docker-compose.yml` 必须满足：
- 顶层 `name: water-leak`
- 所有服务使用 `container_name`，命名带 `${COMPOSE_PROJECT_NAME:-water-leak}` 前缀
- 前端端口映射：`${FRONTEND_PORT:-20105}:80`
- 后端端口映射：`${BACKEND_PORT:-21105}:8080`
- 前端代码里统一请求 `/api`，禁止硬编码 `localhost`

启动后要求：
- `docker compose config --quiet` 不报错
- `docker compose up -d` 后前端访问 `http://localhost:20105`
- 后端健康检查访问 `http://localhost:21105/health`
- README 首选启动命令必须是 `cp .env.example .env && docker compose up -d`


### README 写法

根目录必须有中文 `README.md`，且 Docker 启动方式放在最前面。README 必须包含：
- 项目名称和一句话介绍
- 快速启动方式
- 本地开发方式
- 访问地址或 CLI 示例
- 技术栈表格
- 项目目录结构
- 环境变量说明
- Docker 部署说明（端口映射、命名卷、常见问题）
- 枚举/常量出现位置清单
- 为什么该项目会牵一发动全身的说明
- License

### Git 要求

代码编写完成后初始化 Git，并创建 `.gitignore`，至少包含：
```text
node_modules/
dist/
build/
.env
.DS_Store
*.log
.idea/
.vscode/
coverage/
__pycache__/
```
